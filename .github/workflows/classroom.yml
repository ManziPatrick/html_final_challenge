name: Autograding HTML Challenge

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout student code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: sudo apt-get install -y grep

  autograding:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout student code
        uses: actions/checkout@v3

      - name: Run tests
        uses: education/autograding@v1
